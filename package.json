{
  "name": "htzbuild",
  "version": "1.1.1",
  "description": "Hetzner-based EAS remote builder that can be run from any project root with a shared .env directory.",
  "main": "src/cli.js",
  "type": "commonjs",
  "bin": {
    "htzbuild": "./bin/htzbuild"
  },
  "files": [
    "bin/",
    "src/",
    "cloud-init-builder.yaml",
    "README.md",
    "htzbuild.config.example.json"
  ],
  "scripts": {
    "cli": "node ./bin/htzbuild",
    "test": "jest"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "chalk": "^4.1.2",
    "jest": "^30.2.0",
    "ora": "^5.4.1"
  }
}
